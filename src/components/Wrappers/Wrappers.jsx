import styled from "styled-components";
import { space, layout, color, flexbox, typography, border, shadow } from "styled-system"

export const Div = styled('div')(color, space, layout, flexbox, typography, border, shadow);

export const P = styled('p')(color, space, layout, flexbox, typography);